import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onemoney.org',
  appName: 'omemoney',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
